@model IEnumerable<Glamboyant.Models.ReviewModels.ReviewListItem>
@using Microsoft.AspNet.Identity;
@{
    ViewBag.Title = "Index";
}

<h2 style="        margin-top: 60px;
        text-align: center;
        color: hotpink;
        font-family: 'Megrim', cursive;
        font-weight:600;
">
    REVIEWS
</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { style = "color:hotpink" })
</p>
@if (TempData.ContainsKey("SaveResult"))
{
    <div class="alert alert-success fade in" role="alert">

        <button type="button" class="close" data-dismiss="alert">
            <span>&times;</span>
        </button>
        @TempData["SaveResult"]
    </div>
}
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserID)
        </th>
        <th></th>
    </tr>*@

<div class="row text-center">
    @foreach (var item in Model)
    {
        <div class="card col-md-4" style="min-height: 350px; width: 230px; padding: 5px 5px 5px 5px; margin: 1em 1em 1em 1em; box-shadow: 0 0 5px 3px hotpink; margin-right: 45px;">
            <div style="padding-top:5px;">

                <img src="/Review/RetrieveImage/@item.ReviewID" alt="" height=250 width=200 />
            </div>
            <div>

            @Html.DisplayFor(modelItem => item.Rating)
            </div>
            <div>

                @Html.DisplayFor(modelItem => item.Text)
            </div>

            <div>

                @User.Identity.GetUserName()
            </div>
                @*@Html.DisplayFor(modelItem => item.UserID)*@

            <div>
                @Html.ActionLink("Edit", "Edit", new { id = item.ReviewID }, new { style = "color:hotpink" }) |
                @Html.ActionLink("Details", "Details", new { id = item.ReviewID }, new { style = "color:hotpink" }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ReviewID }, new { style = "color:hotpink" })
            </div>

        </div>
        }
</div>
@*</table>*@
